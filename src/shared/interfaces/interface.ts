interface ILogin {
    nickname: string,
    password: string
}

interface IBaseSize {
    width?: number,
    height?: number
}

interface IControl {
    id: number,
    path: string,
    name: string
}